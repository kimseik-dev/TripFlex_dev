/** @type {import('next').NextConfig} */
const nextConfig = {
  // v450: Root-level placeholder config for Vercel Monorepo detection
  // This file tells Vercel that this is a Next.js project.
};

export default nextConfig;
